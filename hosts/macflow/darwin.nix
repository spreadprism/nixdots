{ lib, pkgs, stateVersion, username, hostname, ... }:
{
  environment.systemPackages = with pkgs;
  [
  ];

  homebrew = {
    brews = [

    ];

    casks = [

    ];

    masApps = {

    };
  };
}
